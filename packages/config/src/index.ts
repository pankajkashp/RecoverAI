/**
 * @recoverai/config
 *
 * Shared configuration constants for RecoverAI.
 *
 * This package provides configuration values that are shared
 * across the frontend, backend, and other workspace packages.
 *
 * Phase 1: package boundary established.
 */

/** Default port for the backend API server. */
export const DEFAULT_BACKEND_PORT = 4000;

/** Default port for the frontend development server. */
export const DEFAULT_FRONTEND_PORT = 3000;

/** Default port for the ML service. */
export const DEFAULT_ML_SERVICE_PORT = 8000;
